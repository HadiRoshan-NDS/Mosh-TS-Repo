//Literals
let quantityNum: 50 | 100 = 50;

/* Instead of hard-coding the values, we can use a type alias */
type Quantity = 10 | 20 | 30 |40 | 50
let quantityNum2: Quantity = 40;

/* Literals can also be string s instead of numbers, like:
type Metric = 'cm' | 'inch' */

